1️⃣ What is the difference between var, let, and const?

The value of var and let can change but const cannot. And between var and let, let lets you reassign the value. 
2️⃣ What is the spread operator (...)?
spread operator expands arrays, strings or objects into individual element.

3️⃣ What is the difference between map(), filter(), and forEach()?

fliter() function returns all of the objects that are similar to the value.

But map() function creates a new set of arrays that are similar to the value.

with forEach() function, an event can be executed for all of the elements once.



4️⃣ What is an arrow function?
arrow function is a collapsed method of writing function.
5️⃣ What are template literals?
 These are template literals ``.